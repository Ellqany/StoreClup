@model WebGUI.Models.Manage
@{
    Layout = null;
    AjaxOptions ajax = new AjaxOptions
    {
        OnSuccess = "Success2",
        AllowCache = true,
        HttpMethod = "POST",
        UpdateTargetId = "tbl",
        OnFailure = "Failure2"
    };
}

<div class="w3-modal-content w3-card-4 w3-animate-zoom" style="padding:32px;">
    @using (Ajax.BeginForm("Edit", "Account", null, ajax, null))
    {
        <div class="w3-container w3-white w3-center">
            <i onclick="document.getElementById('AddressData').style.display='none'" class="fa fa-remove w3-right w3-button w3-transparent w3-xxlarge"></i>
            <h2 class="w3-wide">Add or Edit your Address</h2>
            <h4 class="text-danger" id="failuer1">@TempData["Error"]</h4>
            <h4 class="alert-success" id="Success1">@TempData["success"]</h4>
            @Html.HiddenFor(x => x.Billing)
            @Html.HiddenFor(x => x.PhoneNumber)
            @Html.HiddenFor(x => x.Email)
            <p>
                @Html.TextBoxFor(x => x.Address, new { @placeholder = "Add Your Address", @class = "form-control" })
                @Html.ValidationMessageFor(x => x.Address, "", new { @class = "text-danger" })
            <p>
                <br />
                <button type="submit" class="w3-button w3-padding-large w3-red w3-margin-bottom ">
                    <i class="fa fa-plus-square"></i>
                </button>
        </div>
    }
</div>

<script>
    function Success2() {
        document.getElementById('failuer1').innerHTML = ' ';
        document.getElementById('Success1').innerHTML = 'Address has been updated successfully';
        document.getElementById('AddressData').style.display='none'
    }

    function Failure2() {
        document.getElementById('failuer1').innerHTML = 'there is an error occure while trying to attemp your request';
        document.getElementById('Success1').innerHTML = ' ';
    }
</script>
